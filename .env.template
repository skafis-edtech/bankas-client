SDK_LOCAL_PATH=path/to/firebase-admin.json # Path to the Firebase Admin SDK JSON file, generated by firebase
BACKUP_PATH=path/to/folder/backups # Path to local folder where backups will be stored in JSON format
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com # Firebase storage bucket for backups.